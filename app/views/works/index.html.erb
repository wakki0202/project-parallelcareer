



<body class="providerPage">
  <div class="side">
    <%= render '/works/side' %>
  </div>
 
  <div class="main">
    <div class="mainHeader">
    <ul>
    <% if provider_signed_in? %>
      <li class=""><span><%= link_to '事業者ログアウト' , destroy_provider_session_path, method: :delete,data: { confirm: 'ログアウトしますか？' } %></span></li> 
      <li><%= current_provider.email %></li>
      <% else %>
      <li class=""><span>ログアウト</span></li> 
      <li>管理者名</li>
    
      <% end %>

    </ul>
    </div>
    
        
    <div class="userIndex">
      <h3 class="text-center">案件一覧</h3>
      <div class="providerTable">
      <a href="works/new" class="toNew">＋ 新規作成</a>

        <table class="table userTable">

        <thead class="heading">
          <tr class="border">
            <th class="border">案件名</th>
            <th>ステータス</th>
            <th></th>
            <th></th>
          </tr>
        </thead>

        <tbody>
          <% @works.each do |work| %>
            <tr class="">
              <td class=""><%= work.title %></td>
              <td class=""><%= work.status %></td>
              <td class=""><%= link_to '詳細', work,class:"btn" %></td>
              <td class=""><%= link_to '編集', edit_work_path(work),class:"btn" %></td>
              
            </tr>
          <% end %>
        </tbody>
      </table>
      <%= paginate @works %>
      </div>
    </div>

  </div>
</body>

   